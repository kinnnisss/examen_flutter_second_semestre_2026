# Icône de lancement

Placez ici le logo de l'application sous le nom `icon.png` (1024×1024 recommandé),
puis générez les icônes natives :

```bash
flutter pub run flutter_launcher_icons
```

La configuration se trouve dans `pubspec.yaml` (section `flutter_launcher_icons`).
